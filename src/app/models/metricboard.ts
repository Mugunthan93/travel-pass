export interface metricBoard {
    id: number
    type_of_booking: string
    customer_id: number
    user_id: number
    sector: sector
    updatedAt: string
    createdAt: string
}

export interface sector {
    
}